//your JS code here. If required.
function functionName() {
    // Display the function's own name using arguments.callee.name
    alert(arguments.callee.name);
}

// Call the function to see the result
functionName();